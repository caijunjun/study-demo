
#查看安装包
rpm -qa|grep -i mysql 

#删除安装包
rpm -e --nodeps mysql包名 


#安装mysql
#FATAL ERROR: please install the following Perl modules before executing /usr/bin/mysql_install_db: 
#yum install -y perl-Module-Install.noarch

#安装服务端
rpm -ivh --replacefiles MySQL-server-5.6.40-1.el7.x86_64.rpm
#安装客户端
rpm -ivh --replacefiles MySQL-client-5.6.40-1.el7.x86_64.rpm

#启动服务，需要 提前删除/etc/my.cnf  不管什么地方的 都最好删除
service mysql start
#Starting MySQL. ERROR! The server quit without updating PID file (/var/lib/mysql/localhost.localdomain.pid).

#切换数据源存放地址
mysql_install_db --user=mysql --basedir=/usr --datadir=/data/mysql


#chown -R mysql:mysql /database/mysql
#chmod -R 755 /database/mysql/data





#初始化mysql用户信息

mysql;
use mysql;
desc user;
select Host, User, Password from user where User='root';
update user set Password=password('root') where User='root';

#pwd为远程客户端访问密码
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' IDENTIFIED BY 'pwd' WITH GRANT OPTION;
flush privileges;
mysqladmin -u root -p password root;


RHEL7 中使用了firewalld代替了原来的iptables，操作设置和原来有点不同：
查看防火墙状态：systemctl status firewalld
启动防火墙：systemctl start firewalld
停止防火墙：systemctl stop firewalld

#方式一
firewall-cmd --zone=public --permanent --add-port=3306/tcp
firewall-cmd --reload
#方式二
tail /etc/firewalld/zones/public.xml
vi /etc/firewalld/zones/public.xml